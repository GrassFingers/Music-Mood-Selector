#!/bin/sh
# Runs the test suite before allowing a commit. Bypass with `git commit --no-verify`.

python -m pytest
status=$?

if [ $status -ne 0 ]; then
    echo "Tests failed - commit aborted."
    exit 1
fi

exit 0
